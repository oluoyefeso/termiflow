# golangci-lint configuration for termiflow
# Compatible with golangci-lint v1.64+

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - bodyclose

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: false
  govet:
    enable-all: true
    disable:
      - fieldalignment
      - shadow
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/oluoyefeso/termiflow
  misspell:
    locale: US

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - bodyclose
    - path: _test\.go
      text: "shadow"
    - text: "unused-parameter"
      linters:
        - revive
  max-issues-per-linter: 50
  max-same-issues: 10
